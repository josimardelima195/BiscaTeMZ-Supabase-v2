import { adminSupabase,currentUser } from "@/lib/supabase-server";
async function allowed(id:string,userId:string){const {data}=await adminSupabase().from("conversation_members").select("user_id").eq("conversation_id",id).eq("user_id",userId).maybeSingle();return Boolean(data);}
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser(request),{id}=await params;if(!u||!await allowed(id,u.id))return Response.json({error:"Não autorizado"},{status:401});const {data}=await adminSupabase().from("messages").select("*,sender:profiles!messages_sender_id_fkey(full_name,photo_url)").eq("conversation_id",id).order("created_at");return Response.json({messages:data??[]});}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const u=await currentUser(request),{id}=await params;if(!u||!await allowed(id,u.id))return Response.json({error:"Não autorizado"},{status:401});const {body}=await request.json();if(!String(body??"").trim())return Response.json({error:"Escreva uma mensagem."},{status:400});const {data,error}=await adminSupabase().from("messages").insert({conversation_id:id,sender_id:u.id,body:String(body).trim().slice(0,2000)}).select().single();if(error)return Response.json({error:"Falha ao enviar."},{status:500});return Response.json({message:data},{status:201});}
